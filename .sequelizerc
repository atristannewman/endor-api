const path = require("path");

module.exports = {
  config: path.resolve("config", "config.json"),
  "models-path": path.resolve("databases", "postgres", "entity"),
  "seeders-path": path.resolve("databases", "seeders"),
  "migrations-path": path.resolve("migrations"),
};
